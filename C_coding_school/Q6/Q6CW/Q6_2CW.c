#include <stdio.h>
#define ARRAY_SIZE 5

int main()
{
    printf("Enter %d integers:\n", ARRAY_SIZE);
    scanf("%d %d %d %d %d", &arr[0], &arr[1], &arr[2], &arr[3], &arr[4]);

    return 0;
}

int the_most_common_element(int arr[], int size)
{}